{
  "name": "aws-email-service",
  "version": "1.0.0",
  "description": "AWS Lambda function for sending emails with embedded images",
  "main": "index.mjs",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@aws-sdk/client-ses": "^3.525.0",
    "axios": "^1.6.7",
    "node-html-parser": "^6.1.12",
    "nodemailer": "^6.9.10"
  },
  "engines": {
    "node": ">=18.0.0"
  }
} 